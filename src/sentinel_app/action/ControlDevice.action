# Input
int32[] motor_channels       # Canali dei motori da controllare
float32[] motor_angles       # Angoli target per i motori
float32[] motor_speeds       # Velocità dei motori
bool[] relay_states          # Stati dei relay (true = ON, false = OFF)

---
# Feedback (opzionale)
string feedback_message       # Messaggi sullo stato dell'operazione

---
# Risultato
bool success                  # Indica se l'azione è riuscita
string result_message         # Messaggio finale di risultato
